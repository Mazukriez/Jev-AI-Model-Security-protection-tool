{
  "name": "jevshield",
  "version": "0.2.0",
  "description": "Open-source security protection layer for typed AI decision workflows such as Jev.",
  "private": false,
  "type": "module",
  "main": "src/index.js",
  "bin": { "jevshield": "src/cli.js" },
  "scripts": {
    "test": "node --test",
    "lint": "node --check src/index.js && node --check src/cli.js && node --check src/api/server.js",
    "start": "node src/api/server.js",
    "dev": "node src/api/server.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
